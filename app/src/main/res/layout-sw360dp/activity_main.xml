<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:fitsSystemWindows="false">

    <!-- AppListView component optimized for standard phones (360dp+) -->
    <com.example.m_launcher.AppListView
        android:id="@+id/app_list_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:minWidth="@dimen/material_expressive_app_list_min_width"
        android:layout_margin="@dimen/material_expressive_padding_medium" />
    
</FrameLayout>